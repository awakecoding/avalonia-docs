# Avalonia.Data.ReflectionBinding

A binding that uses reflection to access members.

## Metadata

- Kind: Class
- Namespace: [Avalonia.Data](../../namespaces/avalonia-data.md)
- Assemblies: Avalonia.Base

## Inheritance

- `System.Object`
- `Avalonia.Data.BindingBase`

## Syntax

```csharp
public class ReflectionBinding : BindingBase
```

## Constructors

| Member | Summary |
| --- | --- |
| `ReflectionBinding.ReflectionBinding()` | Initializes a new instance of the class. |
| `ReflectionBinding.ReflectionBinding(string)` | Initializes a new instance of the class. |

## Properties

| Member | Summary |
| --- | --- |
| `ReflectionBinding.ConverterCulture` | Gets or sets the culture in which to evaluate the converter. |
| `ReflectionBinding.ConverterParameter` | Gets or sets a parameter to pass to . |
| `ReflectionBinding.Delay` | Gets or sets the amount of time, in milliseconds, to wait before updating the binding source after the value on the target changes. |
| `ReflectionBinding.ElementName` | Gets or sets the name of the element to use as the binding source. |
| `ReflectionBinding.FallbackValue` | Gets or sets the value to use when the binding is unable to produce a value. |
| `ReflectionBinding.Mode` | Gets or sets the binding mode. |
| `ReflectionBinding.Path` | Gets or sets the binding path. |
| `ReflectionBinding.Priority` | Gets or sets the binding priority. |
| `ReflectionBinding.RelativeSource` | Gets or sets the relative source for the binding. |
| `ReflectionBinding.Source` | Gets or sets the source for the binding. |
| `ReflectionBinding.StringFormat` | Gets or sets the string format. |
| `ReflectionBinding.TargetNullValue` | Gets or sets the value to use when the binding result is null. |
| `ReflectionBinding.TypeResolver` | Gets or sets a function used to resolve types from names in the binding path. |
| `ReflectionBinding.UpdateSourceTrigger` | Gets or sets a value that determines the timing of binding source updates for and bindings. |

