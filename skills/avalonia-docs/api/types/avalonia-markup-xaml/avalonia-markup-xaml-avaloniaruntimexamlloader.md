# Avalonia.Markup.Xaml.AvaloniaRuntimeXamlLoader

## Metadata

- Kind: Class
- Namespace: [Avalonia.Markup.Xaml](../../namespaces/avalonia-markup-xaml.md)
- Assemblies: Avalonia.Markup.Xaml.Loader

## Inheritance

- `System.Object`

## Syntax

```csharp
public static class AvaloniaRuntimeXamlLoader
```

## Methods

| Member | Summary |
| --- | --- |
| `AvaloniaRuntimeXamlLoader.Load(RuntimeXamlLoaderDocument, RuntimeXamlLoaderConfiguration?)` | Loads XAML from a stream. |
| `AvaloniaRuntimeXamlLoader.Load(Stream, Assembly?, object?, Uri?, bool)` | Loads XAML from a stream. |
| `AvaloniaRuntimeXamlLoader.Load(string, Assembly?, object?, Uri?, bool)` | Loads XAML from a string. |
| `AvaloniaRuntimeXamlLoader.LoadGroup(IReadOnlyCollection<RuntimeXamlLoaderDocument>, RuntimeXamlLoaderConfiguration?)` | Loads group of XAML files from a stream. |
| `AvaloniaRuntimeXamlLoader.Parse(string, Assembly?)` | Parse XAML from a string. |
| `AvaloniaRuntimeXamlLoader.Parse<T>(string, Assembly?)` | Parse XAML from a string. |

