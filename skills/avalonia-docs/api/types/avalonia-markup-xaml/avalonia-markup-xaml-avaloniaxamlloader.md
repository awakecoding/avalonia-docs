# Avalonia.Markup.Xaml.AvaloniaXamlLoader

Loads XAML for a avalonia application.

## Metadata

- Kind: Class
- Namespace: [Avalonia.Markup.Xaml](../../namespaces/avalonia-markup-xaml.md)
- Assemblies: Avalonia.Markup.Xaml

## Inheritance

- `System.Object`

## Syntax

```csharp
public static class AvaloniaXamlLoader
```

## Methods

| Member | Summary |
| --- | --- |
| `AvaloniaXamlLoader.Load(IServiceProvider?, object)` | Loads the XAML into a Avalonia component. |
| `AvaloniaXamlLoader.Load(IServiceProvider?, Uri, Uri?)` | Loads XAML from a URI. |
| `AvaloniaXamlLoader.Load(object)` | Loads the XAML into a Avalonia component. |
| `AvaloniaXamlLoader.Load(Uri, Uri?)` | Loads XAML from a URI. |

